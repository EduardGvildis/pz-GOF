```markdown
# Патерн Сінглтон (Singleton Pattern)

## Що це таке?

Патерн Сінглтон гарантує, що в програмі існує лише один екземпляр певного класу, і забезпечує глобальну точку доступу до нього.

## Коли використовувати?

- Коли потрібно централізувати доступ до спільного ресурсу (наприклад, логер, пул підключень)
- Коли має бути лише один екземпляр класу під час виконання програми
- Коли треба контролювати життєвий цикл одного об'єкта

## Структура

1. **Клас (Singleton)** - містить приватний конструктор і статичний метод для отримання екземпляра
2. **Клієнт (Client)** - використовує статичний метод для доступу до єдиного екземпляра

## Приклад використання

```typescript
import { LoggerSingleton } from './index';

const s1 = LoggerSingleton.getInstance();
s1.log('Початок роботи');

const s2 = LoggerSingleton.getInstance();
s2.log('Ще один запис');

console.log(s1 === s2); // true
console.log(s1.getLogs());
```

## Переваги

- **Єдина точка доступу**: зручно управляти спільним ресурсом
- **Легкість доступу**: не потрібно передавати екземпляр по всьому коду

## Недоліки

- Ускладнює тестування через глобальний стан
- Може призвести до неочікуваних залежностей між компонентами
- Неправильна реалізація (наприклад, глобальний мутабельний об'єкт) — анти-патерн

## Анти-патерн

- Експорт глобального мутабельного об'єкта як "сінглтон" ускладнює контроль життєвого циклу та тестування.

## Реалізація в цьому проекті

Дивись файли:
- `index.ts` - реалізація патерну
- `example.ts` - приклад використання
- `anti-example.ts` - анти-приклад (глобальний мутабельний об'єкт)
```
# Патерн Будівельник (Builder Pattern)

## Що це таке?

Патерн Будівельник відокремлює конструювання складного об'єкта від його представлення, дозволяючи створювати різні представлення одного й того самого процесу конструювання.

## Коли використовувати?

- Коли процес створення об'єкта включає багато кроків
- Коли потрібно створювати різні представлення об'єкта
- Коли конструктор з великою кількістю параметрів стає незручним
- Коли хочете забезпечити незмінність об'єкта після створення

## Структура

1. **Продукт (Product)** - складний об'єкт, що створюється
2. **Будівельник (Builder)** - абстрактний інтерфейс для створення продукту
3. **Конкретний будівельник (Concrete Builder)** - реалізація будівельника
4. **Директор (Director)** - керує процесом створення за допомогою будівельника
5. **Клієнт (Client)** - використовує директора для створення продукту

## Приклад використання

```typescript
// Створення будівельника та директора
const builder = new MargheritaPizzaBuilder();
const director = new PizzaDirector(builder);

// Конструювання піци через покроковий процес
const pizza = director.constructPizza();
console.log(pizza.getDescription());
```

## Переваги

- **Контроль процесу створення**: Покрокове конструювання складних об'єктів
- **Гнучкість**: Можна створювати різні представлення одного продукту
- **Читабельність**: Код створення більш зрозумілий, ніж конструктори з багатьма параметрами
- **Незмінність**: Продукт можна зробити незмінним після створення

## Недоліки

- Збільшує кількість класів у системі
- Будівельник та продукт тісно зв'язані
- Не підходить для простих об'єктів

## Реалізація в цьому проекті

Дивись файли:
- `index.ts` - реалізація патерну
- `example.ts` - приклад використання</content>
<parameter name="filePath">c:\Users\sasha\IdeaProjects\untitled\pz-GOF\src\creational\builder\README.md